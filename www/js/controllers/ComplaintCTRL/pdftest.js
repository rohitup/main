angular.module('starter')

  .controller('pdftestctrl', function ($scope, $location, RestService, $ionicLoading, $stateParams, toaster,
		  $filter, ENV, dateFilter, $state,sharedProperties, $localStorage) {
	  
	  
	  
	  
  }) /*controler ends*/
  
